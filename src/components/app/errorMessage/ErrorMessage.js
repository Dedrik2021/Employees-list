import img from '../errorMessage/error.webp'

const ErrorMessage = () => {
    return (
        <img src={img} style={{height: '200px', width: '100%', margin: '0 auto'}}/>
    )
}

export default ErrorMessage